import { useEffect } from "react";

// a new app that connects to an api: mealdb, we loop through it and display it on our api
const Meal = ()=>{

    useEffect(()=>{
        console.log("Hello World")
    })
    return(
        <div>Meal</div>
    )
}

export default Meal;